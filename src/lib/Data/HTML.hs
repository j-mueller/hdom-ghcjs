{-# LANGUAGE OverloadedStrings #-}
module Data.HTML(
  module Data.DOM.HTML,
  Handler,
  module Data.DOM
  ) where

import           Data.DOM
import           Data.DOM.HTML
import           Data.VDOM.Internal.Types (Handler)
